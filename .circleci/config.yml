version: 2
jobs:
  build:
    docker:
      - image: circleci/golang
    steps:
      - checkout
      - run: ls -alh
  test:
    docker:
      - image: circleci/golang
    steps:
      - checkout
      - run: ls -alh
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
